# Runtime Playbook

Record only confirmed, reusable runtime and tooling failure patterns.

For each pattern include:

- a stable identifier;
- recognizable symptoms;
- likely scope or cause;
- safe diagnostic steps;
- known response;
- validation and reporting boundaries.

Do not include full raw logs, private paths, credentials, or unverified guesses.
